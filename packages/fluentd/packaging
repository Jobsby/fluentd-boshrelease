set -e

source /var/vcap/packages/ruby-3.0.3-r0.79.0/bosh/compile.env

cp -R fluentd/{Gemfile,Gemfile.lock,vendor} ${BOSH_INSTALL_TARGET}
cd ${BOSH_INSTALL_TARGET}

bundle install \
  --local \
  --deployment \
  --without development test \
  --binstubs
